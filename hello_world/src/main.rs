// create project: cargo new project_name --bin
// run project: cargo run
// build project: cargo build ->> target/debug
// build release project: cargo build --release ->> target/release
fn main() {
    println!("Hello, world!");
}
